export default async function handler(req,res){const {name,email,message}=req.body||{};if(req.method!=='POST'||!String(name||'').trim()||!/^\S+@\S+\.\S+$/.test(email||'')||String(message||'').trim().length<3)return res.status(400).json({error:'Complete all fields'});if(process.env.RESEND_API_KEY)await fetch('https://api.resend.com/emails',{method:'POST',headers:{authorization:`Bearer ${process.env.RESEND_API_KEY}`,'content-type':'application/json'},body:JSON.stringify({from:process.env.EMAIL_FROM,to:[process.env.CONTACT_TO],reply_to:email,subject:`Blueprint consultation: ${name}`,text:message})});else console.info('Consultation request',{name,email,message});res.status(200).json({received:true})}

import {generate} from './core.mjs';export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{if(!req.body?.answers||typeof req.body.answers!=='object')return res.status(400).json({error:'Answers are required'});res.status(200).json(await generate(req.body.answers))}catch{res.status(500).json({error:'Blueprint generation failed'})}}

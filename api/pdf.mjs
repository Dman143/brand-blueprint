import {pdf,validBlueprint} from './core.mjs';export default function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(!validBlueprint(req.body?.blueprint))return res.status(400).json({error:'Invalid Blueprint'});res.setHeader('content-type','application/pdf');res.setHeader('content-disposition','attachment; filename="brand-blueprint.pdf"');res.status(200).send(pdf(req.body.blueprint))}
